{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile TS files to script.js file.",
            "type": "typescript",
            "tsconfig": "tsconfig.json",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "runOptions": {
                //"runOn": "folderOpen"
            }
        },

        //run grunt (gruntfile.js) to minify script.js and styles.css files.
        //works for app and sales pages
        {
            "label": "grunt-minify",
            "command": "grunt",
            "type": "shell",
            "problemMatcher": [],
        }
    ]
}